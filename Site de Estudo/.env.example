# Copie este arquivo para ".env" e preencha com a sua connection string real.
# O .env nunca deve ser enviado ao GitHub (já está no .gitignore).
DATABASE_URL=postgresql://usuario:senha@host/banco?sslmode=require&channel_binding=require
